import React from 'react';
import {
  Text,
  View,
} from 'react-native';

const ScanScreen = () => {
  return (
    <View style={{justifyContent: 'center'}}>
      <Text>
        Scan
      </Text>
    </View>
  );
};

export default ScanScreen;
